// Pinned as a set. compileSdk 36 needs AGP 8.9+, and AGP 8.9 needs Gradle
// 8.11.1+, which is set in the workflow. Moving one without the others fails.
plugins {
    id 'com.android.application' version '8.9.2' apply false
    id 'org.jetbrains.kotlin.android' version '2.0.21' apply false
}
